{
  "name": "cyoda-test-management-system",
  "version": "0.1.0",
  "private": true,
  "description": "Cyoda TMS — monorepo containing the React frontend and Spring Boot backend",
  "scripts": {
    "dev":              "bash start-dev.sh",
    "dev:frontend":     "npm --prefix frontend run dev",
    "dev:backend":      "cd backend && bash RUN_WITH_CYODA.sh",
    "build":            "npm run build:frontend && npm run build:backend",
    "build:frontend":   "npm --prefix frontend run build",
    "build:backend":    "cd backend && ./gradlew clean build -x test",
    "test":             "npm run test:frontend && npm run test:backend",
    "test:frontend":    "npm --prefix frontend run test",
    "test:backend":     "cd backend && ./gradlew test",
    "install:frontend": "npm --prefix frontend install",
    "lint:frontend":    "npm --prefix frontend run lint"
  },
  "engines": {
    "node": ">=18",
    "npm": ">=9"
  }
}
